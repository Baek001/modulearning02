<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mybatis.mapper.UsersMapper">

    <sql id="userColumns">
        U.USER_ID,
        U.TENANT_ID,
        COALESCE(U.LOGIN_ID, U.USER_ID) AS LOGIN_ID,
        U.USER_PSWD,
        U.USER_NM,
        U.USER_EMAIL,
        U.USER_TELNO,
        U.EXT_TEL,
        U.RSGNTN_YN,
        U.RSGNTN_YMD,
        U.DEPT_ID,
        U.JBGD_CD,
        U.USER_IMG_FILE_ID,
        U.USER_ROLE,
        U.HIRE_YMD,
        U.WORK_STTS_CD,
        TM.TENANT_ROLE_CD,
        TM.MEMBERSHIP_STATUS_CD,
        T.TENANT_NM,
        T.TENANT_SLUG,
        J.JBGD_NM AS jbgdNm,
        D.DEPT_NM AS deptNm,
        C.CODE_NM AS codeNm,
        D.UP_DEPT_ID,
        FD.FILE_PATH
    </sql>

    <insert id="insertUser">
        INSERT INTO USERS(
            USER_ID,
            TENANT_ID,
            LOGIN_ID,
            USER_PSWD,
            USER_NM,
            USER_EMAIL,
            USER_TELNO,
            EXT_TEL,
            RSGNTN_YN,
            RSGNTN_YMD,
            DEPT_ID,
            JBGD_CD,
            USER_IMG_FILE_ID,
            USER_ROLE,
            HIRE_YMD,
            WORK_STTS_CD
        ) VALUES (
            #{userId},
            #{tenantId},
            COALESCE(#{loginId}, #{userId}),
            #{userPswd},
            #{userNm},
            #{userEmail},
            #{userTelno},
            #{extTel},
            'N',
            #{rsgntnYmd},
            #{deptId},
            #{jbgdCd},
            #{userImgFileId},
            COALESCE(#{userRole}, 'ROLE_USER'),
            #{hireYmd},
            COALESCE(#{workSttsCd}, 'C103')
        )
    </insert>

    <select id="selectUserList" resultType="kr.or.ddit.vo.UsersVO">
        SELECT
            <include refid="userColumns"/>
        FROM USERS U
        LEFT JOIN TENANT_MEMBER TM
          ON TM.TENANT_ID = U.TENANT_ID
         AND TM.USER_ID = U.USER_ID
        LEFT JOIN TENANT T
          ON T.TENANT_ID = U.TENANT_ID
        LEFT JOIN JOB_GRADE J
          ON U.JBGD_CD = J.JBGD_CD
        LEFT JOIN DEPARTMENT D
          ON U.DEPT_ID = D.DEPT_ID
        LEFT JOIN COMMON_CODE C
          ON C.CODE_ID = U.WORK_STTS_CD
        LEFT JOIN FILE_DETAIL FD
          ON U.USER_IMG_FILE_ID = FD.FILE_ID
        WHERE COALESCE(U.RSGNTN_YN, 'N') = 'N'
        ORDER BY U.TENANT_ID, U.DEPT_ID, U.JBGD_CD, U.USER_NM
    </select>

    <select id="selectUserListByTenant" resultType="kr.or.ddit.vo.UsersVO">
        SELECT
            <include refid="userColumns"/>
        FROM USERS U
        LEFT JOIN TENANT_MEMBER TM
          ON TM.TENANT_ID = U.TENANT_ID
         AND TM.USER_ID = U.USER_ID
        LEFT JOIN TENANT T
          ON T.TENANT_ID = U.TENANT_ID
        LEFT JOIN JOB_GRADE J
          ON U.JBGD_CD = J.JBGD_CD
        LEFT JOIN DEPARTMENT D
          ON U.DEPT_ID = D.DEPT_ID
        LEFT JOIN COMMON_CODE C
          ON C.CODE_ID = U.WORK_STTS_CD
        LEFT JOIN FILE_DETAIL FD
          ON U.USER_IMG_FILE_ID = FD.FILE_ID
        WHERE U.TENANT_ID = #{tenantId}
          AND COALESCE(U.RSGNTN_YN, 'N') = 'N'
        ORDER BY U.DEPT_ID, U.JBGD_CD, U.USER_NM
    </select>

    <select id="selectUser" resultType="kr.or.ddit.vo.UsersVO">
        SELECT
            <include refid="userColumns"/>
        FROM USERS U
        LEFT JOIN TENANT_MEMBER TM
          ON TM.TENANT_ID = U.TENANT_ID
         AND TM.USER_ID = U.USER_ID
        LEFT JOIN TENANT T
          ON T.TENANT_ID = U.TENANT_ID
        LEFT JOIN JOB_GRADE J
          ON U.JBGD_CD = J.JBGD_CD
        LEFT JOIN DEPARTMENT D
          ON U.DEPT_ID = D.DEPT_ID
        LEFT JOIN COMMON_CODE C
          ON C.CODE_ID = U.WORK_STTS_CD
        LEFT JOIN FILE_DETAIL FD
          ON U.USER_IMG_FILE_ID = FD.FILE_ID
        WHERE U.USER_ID = #{userId}
    </select>

    <select id="selectUserByTenant" resultType="kr.or.ddit.vo.UsersVO">
        SELECT
            <include refid="userColumns"/>
        FROM USERS U
        LEFT JOIN TENANT_MEMBER TM
          ON TM.TENANT_ID = U.TENANT_ID
         AND TM.USER_ID = U.USER_ID
        LEFT JOIN TENANT T
          ON T.TENANT_ID = U.TENANT_ID
        LEFT JOIN JOB_GRADE J
          ON U.JBGD_CD = J.JBGD_CD
        LEFT JOIN DEPARTMENT D
          ON U.DEPT_ID = D.DEPT_ID
        LEFT JOIN COMMON_CODE C
          ON C.CODE_ID = U.WORK_STTS_CD
        LEFT JOIN FILE_DETAIL FD
          ON U.USER_IMG_FILE_ID = FD.FILE_ID
        WHERE U.USER_ID = #{userId}
          AND U.TENANT_ID = #{tenantId}
    </select>

    <update id="updateUser">
        UPDATE USERS
        SET USER_PSWD = #{userPswd},
            USER_NM = #{userNm},
            USER_EMAIL = #{userEmail},
            USER_TELNO = #{userTelno},
            EXT_TEL = #{extTel},
            DEPT_ID = #{deptId},
            JBGD_CD = #{jbgdCd},
            LOGIN_ID = COALESCE(#{loginId}, LOGIN_ID)
        WHERE USER_ID = #{userId}
    </update>

    <update id="updateUserByTenant">
        UPDATE USERS
        SET USER_PSWD = #{userPswd},
            USER_NM = #{userNm},
            USER_EMAIL = #{userEmail},
            USER_TELNO = #{userTelno},
            EXT_TEL = #{extTel},
            DEPT_ID = #{deptId},
            JBGD_CD = #{jbgdCd},
            LOGIN_ID = COALESCE(#{loginId}, LOGIN_ID)
        WHERE USER_ID = #{userId}
          AND TENANT_ID = #{tenantId}
    </update>

    <update id="retireUser">
        UPDATE USERS
        SET RSGNTN_YN = 'Y',
            RSGNTN_YMD = CURRENT_TIMESTAMP
        WHERE USER_ID = #{userId}
    </update>

    <update id="retireUserByTenant">
        UPDATE USERS
        SET RSGNTN_YN = 'Y',
            RSGNTN_YMD = CURRENT_TIMESTAMP
        WHERE USER_ID = #{userId}
          AND TENANT_ID = #{tenantId}
    </update>

    <select id="selectUsersByTerm" resultType="kr.or.ddit.vo.UsersVO">
        SELECT
            U.USER_ID,
            U.TENANT_ID,
            COALESCE(U.LOGIN_ID, U.USER_ID) AS LOGIN_ID,
            U.USER_NM,
            U.USER_EMAIL,
            U.USER_TELNO,
            U.EXT_TEL,
            U.HIRE_YMD,
            J.JBGD_NM,
            COALESCE(U.RSGNTN_YN, 'N') AS RSGNTN_YN,
            D.DEPT_ID,
            D.DEPT_NM AS deptNm,
            FD.FILE_PATH
        FROM USERS U
        LEFT JOIN DEPARTMENT D
          ON U.DEPT_ID = D.DEPT_ID
        LEFT JOIN JOB_GRADE J
          ON U.JBGD_CD = J.JBGD_CD
        LEFT JOIN FILE_DETAIL FD
          ON U.USER_IMG_FILE_ID = FD.FILE_ID
        WHERE U.RSGNTN_YN = 'N'
          AND (
                UPPER(U.USER_NM) LIKE '%' || UPPER(#{term}) || '%'
                OR UPPER(D.DEPT_NM) LIKE '%' || UPPER(#{term}) || '%'
                OR UPPER(U.USER_ID) LIKE '%' || UPPER(#{term}) || '%'
                OR UPPER(U.USER_EMAIL) LIKE '%' || UPPER(#{term}) || '%'
                OR UPPER(U.USER_TELNO) LIKE '%' || UPPER(#{term}) || '%'
              )
        ORDER BY U.USER_NM
    </select>

    <select id="selectUsersByTermInTenant" resultType="kr.or.ddit.vo.UsersVO">
        SELECT
            U.USER_ID,
            U.TENANT_ID,
            COALESCE(U.LOGIN_ID, U.USER_ID) AS LOGIN_ID,
            U.USER_NM,
            U.USER_EMAIL,
            U.USER_TELNO,
            U.EXT_TEL,
            U.HIRE_YMD,
            J.JBGD_NM,
            COALESCE(U.RSGNTN_YN, 'N') AS RSGNTN_YN,
            D.DEPT_ID,
            D.DEPT_NM AS deptNm,
            FD.FILE_PATH
        FROM USERS U
        LEFT JOIN DEPARTMENT D
          ON U.DEPT_ID = D.DEPT_ID
        LEFT JOIN JOB_GRADE J
          ON U.JBGD_CD = J.JBGD_CD
        LEFT JOIN FILE_DETAIL FD
          ON U.USER_IMG_FILE_ID = FD.FILE_ID
        WHERE U.TENANT_ID = #{tenantId}
          AND U.RSGNTN_YN = 'N'
          AND (
                UPPER(U.USER_NM) LIKE '%' || UPPER(#{term}) || '%'
                OR UPPER(D.DEPT_NM) LIKE '%' || UPPER(#{term}) || '%'
                OR UPPER(U.USER_ID) LIKE '%' || UPPER(#{term}) || '%'
                OR UPPER(U.USER_EMAIL) LIKE '%' || UPPER(#{term}) || '%'
                OR UPPER(U.USER_TELNO) LIKE '%' || UPPER(#{term}) || '%'
              )
        ORDER BY U.USER_NM
    </select>

    <select id="selectResignedUserList" resultType="kr.or.ddit.vo.UsersVO">
        SELECT
            U.USER_ID,
            U.TENANT_ID,
            U.USER_NM,
            D.DEPT_NM AS deptNm,
            J.JBGD_NM AS jbgdNm,
            U.USER_EMAIL,
            U.USER_TELNO,
            U.RSGNTN_YN,
            U.RSGNTN_YMD,
            C.CODE_NM AS codeNm
        FROM USERS U
        LEFT JOIN DEPARTMENT D
          ON U.DEPT_ID = D.DEPT_ID
        LEFT JOIN JOB_GRADE J
          ON U.JBGD_CD = J.JBGD_CD
        LEFT JOIN COMMON_CODE C
          ON U.WORK_STTS_CD = C.CODE_ID
        WHERE U.RSGNTN_YN = 'Y'
        ORDER BY U.RSGNTN_YMD DESC
    </select>

    <select id="selectResignedUserListByTenant" resultType="kr.or.ddit.vo.UsersVO">
        SELECT
            U.USER_ID,
            U.TENANT_ID,
            U.USER_NM,
            D.DEPT_NM AS deptNm,
            J.JBGD_NM AS jbgdNm,
            U.USER_EMAIL,
            U.USER_TELNO,
            U.RSGNTN_YN,
            U.RSGNTN_YMD,
            C.CODE_NM AS codeNm
        FROM USERS U
        LEFT JOIN DEPARTMENT D
          ON U.DEPT_ID = D.DEPT_ID
        LEFT JOIN JOB_GRADE J
          ON U.JBGD_CD = J.JBGD_CD
        LEFT JOIN COMMON_CODE C
          ON U.WORK_STTS_CD = C.CODE_ID
        WHERE U.TENANT_ID = #{tenantId}
          AND U.RSGNTN_YN = 'Y'
        ORDER BY U.RSGNTN_YMD DESC
    </select>

    <select id="selectWorkStts" resultType="kr.or.ddit.vo.UsersVO">
        SELECT USER_ID, USER_NM, WORK_STTS_CD
        FROM USERS
        WHERE USER_ID = #{userId}
    </select>

    <update id="updateWorkStts">
        UPDATE USERS
        SET WORK_STTS_CD = #{workSttsCd}
        WHERE USER_ID = #{userId}
    </update>

    <select id="selectUserById" parameterType="string" resultType="kr.or.ddit.vo.UsersVO">
        SELECT USER_ID, TENANT_ID, DEPT_ID, JBGD_CD
        FROM USERS
        WHERE USER_ID = #{userId}
    </select>

    <select id="selectUsersByDept" resultType="kr.or.ddit.vo.UsersVO">
        SELECT
            U.USER_ID,
            U.TENANT_ID,
            U.USER_NM,
            U.RSGNTN_YN,
            U.DEPT_ID,
            U.JBGD_CD,
            U.WORK_STTS_CD
        FROM USERS U
        INNER JOIN (
            WITH RECURSIVE DEPT_TREE AS (
                SELECT DEPT_ID, UP_DEPT_ID
                FROM DEPARTMENT
                WHERE DEPT_ID = #{deptId}
                UNION ALL
                SELECT D.DEPT_ID, D.UP_DEPT_ID
                FROM DEPARTMENT D
                JOIN DEPT_TREE DT
                  ON D.UP_DEPT_ID = DT.DEPT_ID
            )
            SELECT DEPT_ID
            FROM DEPT_TREE
        ) D
          ON U.DEPT_ID = D.DEPT_ID
        WHERE RSGNTN_YN = 'N'
    </select>

    <select id="selectUserForCertificate" parameterType="string" resultType="map">
        SELECT
            U.USER_ID,
            U.USER_PSWD,
            U.USER_NM,
            U.USER_EMAIL,
            U.USER_TELNO,
            U.EXT_TEL,
            U.RSGNTN_YN,
            U.RSGNTN_YMD,
            U.DEPT_ID,
            U.JBGD_CD,
            U.USER_IMG_FILE_ID,
            U.USER_ROLE,
            U.HIRE_YMD,
            U.WORK_STTS_CD,
            U.USER_OTP_SECRET,
            D.DEPT_NM,
            J.JBGD_NM
        FROM USERS U
        LEFT JOIN DEPARTMENT D
          ON U.DEPT_ID = D.DEPT_ID
        LEFT JOIN JOB_GRADE J
          ON U.JBGD_CD = J.JBGD_CD
        WHERE U.USER_ID = #{userId}
          AND U.RSGNTN_YN = 'N'
    </select>
</mapper>
